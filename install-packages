#!/bin/sh

sudo apt-get update
sudo apt-get upgrade -y

# Basic needs
sudo apt-get install -y zsh build-essential linux-headers-generic nginx git software-properties-common python python-software-properties openssl iptables-persistent fail2ban sendmail-bin sendmail

# Let's Encrypt
sudo apt-get install certbot -t jessie-backports
